FROM node:20-alpine

WORKDIR /app

COPY server/package*.json ./server/
RUN cd server && npm install --omit=dev

COPY server ./server

ENV NODE_ENV=production
ENV PORT=4000

EXPOSE 4000

CMD ["node", "server/office-api.js"]
